---
const { post } = Astro.props;
---

<div class="my-8 hover:bg-accent">
	<a href={`/${post.id}/`}>
		<h3 class="text-xl font-bold">{post.data.title}</h3>
		<p>{post.data.length}</p>
		<hr />
	</a>
</div>
